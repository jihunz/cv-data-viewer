version: '3.9'
services:
  megafall-viewer:
    build: .
    ports:
      - "8888:8000"
    volumes:
      - .:/app
      - ${HOST_PATH}:/host
    environment:
      - PYTHONPATH=/app
      - HOST_PATH=${HOST_PATH}
