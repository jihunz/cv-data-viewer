version: '3.9'
services:
  megafall-viewer:
    build: .
    ports:
      - "8888:8000"
    volumes:
      - .:/app
      - ${HOST_PATH:-/Users/Administrator/Downloads}:/host
    environment:
      - PYTHONPATH=/app
      - HOST_HOME=${HOST_PATH:-/Users/Administrator/Downloads}
