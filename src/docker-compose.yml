version: '3.9'
services:
  megafall-viewer:
    build: .
    ports:
      - "8888:8000"
    volumes:
      - .:/app
#      - /Users/jihunjang:/host
      - /Users/Administrator/Downloads:/host
    environment:
      - PYTHONPATH=/app
